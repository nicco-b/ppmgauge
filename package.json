{
  "name": "water-lab",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:unit": "vitest run",
    "test:worker": "vitest run --config vitest.config.workers.ts",
    "smoke": "node scripts/smoke.mjs",
    "seed:local": "node scripts/seed-local.mjs"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "workers-og": "^0.0.25"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.71",
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.6.0",
    "vitest": "2.0.5",
    "wrangler": "^4.98.0"
  }
}
